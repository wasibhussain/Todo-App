class TodosModel {
  String id;
  String title;
  String description;
  bool isCompleted;

  TodosModel(
      {required this.id,
      required this.title,
      required this.description,
      required this.isCompleted});
}
